const $=s=>document.querySelector(s);const measurements=$('#measurements');let lastPrediction=null;
function row(date='',height='',weight=''){const wrap=document.createElement('div');wrap.className='measure-row';wrap.innerHTML=`<div><label>測定日</label><input type="date" class="mdate" value="${date}"></div><div><label>身長(cm)</label><input type="number" class="mheight" inputmode="decimal" min="40" max="220" step="0.1" value="${height}" placeholder="95.2"></div><div><label>体重(kg) 任意</label><input type="number" class="mweight" inputmode="decimal" min="2" max="120" step="0.1" value="${weight}" placeholder="14.2"></div><button type="button" class="delete" aria-label="削除">×</button>`;wrap.querySelector('.delete').addEventListener('click',()=>{if(measurements.children.length>2){wrap.remove();save();}});measurements.appendChild(wrap)}
function isoDateMonthsAgo(n){const d=new Date();d.setMonth(d.getMonth()-n);return d.toISOString().slice(0,10)}
function monthsBetween(a,b){return(b-a)/(1000*60*60*24*30.4375)}function clamp(v,min,max){return Math.min(max,Math.max(min,v))}
const sizes=[50,60,70,80,90,95,100,110,120,130,140,150,160];function baseSize(h){const desired=h+3;return sizes.find(s=>s>=desired)||160}function nextSize(s){const i=sizes.indexOf(s);return sizes[Math.min(i+1,sizes.length-1)]}function prevSize(s){const i=sizes.indexOf(s);return sizes[Math.max(i-1,0)]}
function ageMonths(birth,date){return monthsBetween(new Date(birth+'T12:00:00'),date)}
function interpRef(arr,age){if(!arr||!arr.length)return null;if(age<=arr[0][0])return arr[0][1].slice();for(let i=1;i<arr.length;i++){if(age<=arr[i][0]){const a=arr[i-1],b=arr[i],t=(age-a[0])/(b[0]-a[0]);return a[1].map((v,j)=>v+(b[1][j]-v)*t)}}return arr[arr.length-1][1].slice()}
function refAtAge(age,sex){if(age<0||age>210)return null;if(age<=75)return interpRef(growthRef[sex],age);return interpRef(schoolRef[sex],age)}
function percentileFromHeight(h,refs){if(h<=refs[0])return clamp(3-(refs[0]-h)/(refs[1]-refs[0])*7,1,3);for(let i=1;i<refs.length;i++){if(h<=refs[i]){const t=(h-refs[i-1])/(refs[i]-refs[i-1]);return percentiles[i-1]+(percentiles[i]-percentiles[i-1])*t}}return clamp(97+(h-refs[6])/(refs[6]-refs[5])*3,97,99)}
function heightAtPercentile(refs,p){if(p<=percentiles[0])return refs[0]-(percentiles[0]-p)*(refs[1]-refs[0])/(percentiles[1]-percentiles[0]);for(let i=1;i<percentiles.length;i++){if(p<=percentiles[i]){const t=(p-percentiles[i-1])/(percentiles[i]-percentiles[i-1]);return refs[i-1]+(refs[i]-refs[i-1])*t}}return refs[6]+(p-97)*(refs[6]-refs[5])/(97-90)}
function collectPrediction(){const err=$('#error');err.style.display='none';const targetVal=$('#target').value,birth=$('#birth').value,sex=$('#sex').value;const rows=[...document.querySelectorAll('.measure-row')].map(r=>({date:r.querySelector('.mdate').value,height:parseFloat(r.querySelector('.mheight').value),weight:parseFloat(r.querySelector('.mweight').value)})).filter(x=>x.date&&Number.isFinite(x.height));if(!birth){err.textContent='生年月日を入力してください。';err.style.display='block';return null}if(!sex){err.textContent='発育曲線用の性別を選択してください。';err.style.display='block';return null}if(rows.length<2){err.textContent='身長記録を2件以上入力してください。';err.style.display='block';return null}if(!targetVal){err.textContent='着せたい年月を選んでください。';err.style.display='block';return null}rows.sort((a,b)=>new Date(a.date)-new Date(b.date));const last=rows[rows.length-1],target=new Date(targetVal+'-15T12:00:00'),lastDate=new Date(last.date+'T12:00:00'),ahead=monthsBetween(lastDate,target),targetAge=ageMonths(birth,target);if(ahead<0){err.textContent='着せたい時期は、最新の身長記録より後にしてください。';err.style.display='block';return null}if(ahead>36){err.textContent='予測は3年先までにしてください。';err.style.display='block';return null}if(targetAge<0){err.textContent='着せたい年月が生年月日より前になっています。';err.style.display='block';return null}if(targetAge>210){err.textContent='現在の公的発育データを使った予測は、17歳6か月ごろまでに対応しています。';err.style.display='block';return null}const usable=rows.map(r=>{const age=ageMonths(birth,new Date(r.date+'T12:00:00')),refs=refAtAge(age,sex);return refs?{...r,age,p:percentileFromHeight(r.height,refs)}:null}).filter(Boolean);if(usable.length<2){err.textContent='公的発育曲線に照らせる年齢の身長記録を2件以上入力してください。';err.style.display='block';return null}const recent=usable.slice(-4),weights=recent.map((_,i)=>i+1),ws=weights.reduce((a,b)=>a+b,0),pAvg=recent.reduce((sum,r,i)=>sum+r.p*weights[i],0)/ws,targetRefs=refAtAge(targetAge,sex),predicted=heightAtPercentile(targetRefs,pAvg);const pLow=clamp(pAvg-10,1,99),pHigh=clamp(pAvg+10,1,99),baseLow=heightAtPercentile(targetRefs,pLow),baseHigh=heightAtPercentile(targetRefs,pHigh),extra=0.6+Math.min(ahead,36)*0.04,low=baseLow-extra,high=baseHigh+extra;let standard=baseSize(predicted);const body=$('#bodyType').value;if(body==='sturdy')standard=nextSize(standard);else if(body==='slim'&&$('#fit').value==='just')standard=prevSize(standard);const just=prevSize(standard),loose=nextSize(standard),fit=$('#fit').value,main=fit==='just'?just:fit==='loose'?loose:standard;return{targetVal,rows,last,ahead,predicted,rangeLow:low,rangeHigh:high,standard,just,loose,main,body,pAvg,targetAge,sex}}
function renderPrediction(p){lastPrediction=p;const name=$('#name').value.trim(),[y,m]=p.targetVal.split('-');$('#resultTarget').textContent=`${name?name+'ちゃん・くん ｜ ':''}${y}年${Number(m)}月ごろ`;$('#mainSize').textContent=p.main+'cm';$('#predHeight').textContent=p.predicted.toFixed(1)+'cm';$('#heightRange').textContent=`${p.rangeLow.toFixed(1)}〜${p.rangeHigh.toFixed(1)}cm`;$('#monthsAhead').textContent=Math.round(p.ahead)+'か月';$('#justSize').textContent=p.just+'cm';$('#normalSize').textContent=p.standard+'cm';$('#looseSize').textContent=p.loose+'cm';const weightNote=Number.isFinite(p.last.weight)?` 最新体重は${p.last.weight.toFixed(1)}kgです。体重単独でサイズを決めず、体型指定がある場合だけ少し補正しています。`:'';const sourceNote=p.targetAge<=75?'こども家庭庁の令和5年乳幼児身体発育調査':'こども家庭庁の乳幼児身体発育調査と、文部科学省の学校保健統計';$('#explain').textContent=`生年月日・性別・身長記録を、${sourceNote}の身長パーセンタイルに照らし、その子が走っている発育曲線上の位置をもとに未来の身長を推定しています。${weightNote} 個人差があるため「予測幅」も一緒に見てください。`;$('#result').classList.add('show');save()}
$('#predictBtn').addEventListener('click',()=>{const p=collectPrediction();if(!p)return;renderPrediction(p);$('#result').scrollIntoView({behavior:'smooth',block:'start'})});
$('#judgeBtn').addEventListener('click',()=>{const je=$('#judgeError');je.style.display='none';const p=collectPrediction();if(!p){je.textContent='先に成長記録と着せたい年月を入力してください。';je.style.display='block';return}renderPrediction(p);const itemSize=parseInt($('#itemSize').value,10),type=$('#itemType').value;const typeRoom={top:3,pants:1,outer:8,pajama:4,dress:4}[type]||3;const low=p.rangeLow,high=p.rangeHigh;const lowerFit=itemSize-10-typeRoom,upperFit=itemSize+typeRoom;let level,emoji,headline,detail;if(high<lowerFit){level='maybe';emoji='△';headline='大きめかも';detail=`着る頃の予測身長は約${p.predicted.toFixed(1)}cm。${itemSize}cmはまだ少し大きい可能性があります。長く着たい服なら候補です。`}else if(low>upperFit){level='bad';emoji='×';headline='小さい可能性あり';detail=`着る頃には${itemSize}cmを超えている可能性があります。先買いなら1サイズ上も見てみるのがおすすめです。`}else{level='good';emoji='◎';headline='買ってよさそう！';detail=`着る頃の予測身長は約${p.predicted.toFixed(1)}cm。${itemSize}cmは予測範囲と重なっていて、先買い候補として良さそうです。`}const box=$('#judgeBox');box.className='judge '+level;$('#judgeEmoji').textContent=emoji;$('#judgeHeadline').textContent=headline;$('#judgeDetail').textContent=detail;$('#judgePred').textContent=`予測 ${low.toFixed(0)}〜${high.toFixed(0)}cm`;$('#judgeItem').textContent=`見つけた服 ${itemSize}cm`;const typeName=$('#itemType').selectedOptions[0].textContent;$('#judgeNote').textContent=`${typeName}として少しゆとりを見て判定しています。パンツは丈やウエスト、アウターは重ね着などで差が出やすいため、ブランドのサイズ表も確認してください。`;$('#judgeResult').classList.add('show');$('#judgeResult').scrollIntoView({behavior:'smooth',block:'start'});save()});
function addMeasurement(){row('','','');save()}$('#addBtn').addEventListener('click',addMeasurement);
function save(){const data={name:$('#name').value,birth:$('#birth').value,sex:$('#sex').value,target:$('#target').value,fit:$('#fit').value,bodyType:$('#bodyType').value,itemSize:$('#itemSize').value,itemType:$('#itemType').value,measurements:[...document.querySelectorAll('.measure-row')].map(r=>({date:r.querySelector('.mdate').value,height:r.querySelector('.mheight').value,weight:r.querySelector('.mweight').value}))};localStorage.setItem('kidsize_mvp',JSON.stringify(data))}
function load(){let d=null;try{d=JSON.parse(localStorage.getItem('kidsize_mvp'))}catch(e){}if(d){$('#name').value=d.name||'';$('#birth').value=d.birth||'';$('#sex').value=d.sex||'';$('#target').value=d.target||'';$('#fit').value=d.fit||'normal';$('#bodyType').value=d.bodyType||'unknown';$('#itemSize').value=d.itemSize||'110';$('#itemType').value=d.itemType||'top';if(d.measurements?.length){measurements.innerHTML='';d.measurements.forEach(x=>row(x.date,x.height,x.weight||''));return}}row(isoDateMonthsAgo(12),'','');row(isoDateMonthsAgo(2),'','')}
load();['name','birth','sex','target','fit','bodyType','itemSize','itemType'].forEach(id=>$('#'+id).addEventListener('change',save));$('#clearDataBtn').addEventListener('click',()=>{if(confirm('この端末に保存した入力データを削除しますか？')){localStorage.removeItem('kidsize_mvp');location.reload()}});